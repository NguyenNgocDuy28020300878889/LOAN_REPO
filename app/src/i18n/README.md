# Localization skeleton

The MVP ships with Vietnamese (`vi`) and English (`en`) copy. The initial language follows the device; users can switch it in Preferences.

- Store semantic message keys, never translated financial events, in the database.
- Format money, dates and times on the client according to the active locale.
- Every new screen must be checked in RTL before it is marked done.
